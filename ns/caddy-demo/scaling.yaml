apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  name: caddy
  namespace: caddy-demo 
spec:
  scaleTargetRef:
    deploymentName: caddy
  triggers:
  - type: cpu
    metricType: Utilization 
    metadata:
      type: Utilization 
      value: "30"
  - type: memory
    metricType: Utilization 
    metadata:
      type: Utilization 
      value: "30"
